defaults: &defaults
  s3:
    bucket: bucket_name
    access_key_id: 'access_key'
    secret_access_key: 'secret_key'

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
